name: Deploy to VPS

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 72.61.197.216
          username: hacker
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 42820
          script: /home/hacker/VibeCoding/work/Agentik-Team/scripts/deploy.sh
